public enum #(fieldName) {
	#(enumValues);

	private final #(javaType) value;

    #(fieldName)(final #(javaType) newValue) {
        value = newValue;
    }

    public #(javaType) val() { return value; }
}